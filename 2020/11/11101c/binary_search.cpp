#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    freopen("binary_search.txt", "r", stdin);
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N, K;
    cin >> N >> K;
    cout << N << K;
    return 0;
}