#include <cstring>
#include <iostream>
#include <string>
using namespace std;

int main()
{
    freopen("2743.txt", "r", stdin);
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    string s;
    cin >> s;
    cout << s.size();
    return 0;
}