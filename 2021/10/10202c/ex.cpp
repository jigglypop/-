#include <iostream>
#include <algorithm>
#include <string>
using namespace std;


int main() {
    freopen("6996.txt", "r", stdin);
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    return 0;
}